// @flow
import * as actions from "../constants/ActionTypes";
import { combineReducers } from "redux";

const initialState = {
};

export const inviteReducers = (state = initialState, action) => {
  switch (action.type) {
    default: {
      return state;
    }
  }
};

export default inviteReducers;
